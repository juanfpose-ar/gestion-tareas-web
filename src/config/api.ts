// Re-export from the canonical location to preserve backward compatibility
// during the transition period. Prefer importing from '../api/client' in new code.
export { default } from '../api/client';
